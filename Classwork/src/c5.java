
public class c5 {

	public static void main(String[] args) {
		String theString;
		int theLength;
		theString="The cat sat on the mat.";
		theLength=theString.length(); //method. i am going to use string called string (which is a class and has methods associated with it)
		//length starts from 1 and counts ALL characters. we put parenthesis to send length something if it had needed 
		System.out.print(theLength);
		
		
		
		int d=25;
		System.out.println(Math.sqrt(d)); //saying we need the math class. in the math class, we're using item called square root.
		//these are primitive data types so .sqrt and put math before because we need to import class first
		System.out.println(Math.round(4.5627)); //cant say how many deci places
		System.out.println((int)4.5637);
		// Math.min(arg0, arg1) can choose to put in double or int. many math .s
		//String.
		
		//declaring variables
		int $abc;
		int abc_def; //can use dollar sign and underscores. no periods. no numbers to start
		int pay1;
		int $$$$_$$$_$$$$;
	
		
		

	}

}
